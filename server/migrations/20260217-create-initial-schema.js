'use strict';

// NOTE: this file was split into per-table migrations.
// The original combined implementation is disabled to avoid duplicate actions.
module.exports = {
  up: async () => {
    // noop — use individual migrations in this directory (20260217-0001...)
  },
  down: async () => {
    // noop
  },
};
