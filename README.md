# 📚 Book Review App

ユーザーが書籍をレビュー・管理できるWebアプリケーション。React + Node.js + MySQL で構成。

## クイックスタート

### セットアップ

```bash
# フロントエンド
cd client && npm install && npm run dev

# バックエンド
cd ../server && npm run dev
```

## 📁 プロジェクト構成

- `client/` — React フロントエンド
- `server/` — Express バックエンド
